---
# ansible-galaxy collection install -r requirements.yml
collections:
  # 3.8.0 added env_files to docker_compose_v2.
  - name: community.docker
    version: ">=3.8.0"

  - name: ansible.posix
    version: ">=1.5.0"
