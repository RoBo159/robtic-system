---
# dockerd does not reload daemon.json; live-restore keeps containers up across the restart.
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true
