/** The same bounds the slash command enforces: UTC-12:00 to UTC+14:00, in minutes. */
export const MIN_UTC_OFFSET_MINUTES = -720;
export const MAX_UTC_OFFSET_MINUTES = 840;

/** How many contributors the community panel lists. */
export const TOP_CONTRIBUTORS = 10;
