# Robtic — environment. Copy to .env and fill in the Required block; the rest have working defaults.
#
#   cp .env.example .env
#
# On the server this file lives at /home/robtic/robtic-system/.env, is edited by hand, and is shared
# by every container. Recreate containers after editing it — Compose reads it at create time:
#   docker compose -f infra/docker/compose/docker-compose.yml -p robtic-system \
#     --env-file /home/robtic/robtic-system/.env up -d --force-recreate


# ─── Required ────────────────────────────────────────────────────────────────────────────────────

# production reads MainBotToken; anything else reads TestBot. Set it deliberately.
NODE_ENV=production

MONGODB_URI=mongodb://user:password@host:27017/robtic

# One Discord application for everything. The bot logs in with this token and the dashboard reads
# guild roles/channels with it — there is no second bot token anywhere.
MainBotToken=
TestBot=

# Discord Developer Portal → your application. Add <DASHBOARD_API_URL>/auth/callback to the
# OAuth2 redirect URIs, or dashboard login fails.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Signs session cookies — `openssl rand -hex 32`. Changing it signs everybody out.
DASHBOARD_SESSION_SECRET=


# ─── Bot ─────────────────────────────────────────────────────────────────────────────────────────
#
# Which servers the bot may stay in is NOT an environment variable — it lives in MongoDB and is
# managed from Discord with `!addserver <id>` / `!removeserver <id>`.

# Your Discord user id — bypasses every permission check, so treat it as a credential.
BOT_OWNER_ID=

# Registers slash commands to this one guild (instant) instead of globally (up to an hour).
# Leave blank in production.
COMMAND_GUILD_ID=

# Optional, shown by !ip and !status.
ROBTIC_WEBSITE_URL=
ROBTIC_DISCORD_INVITE=


# ─── Minecraft API ───────────────────────────────────────────────────────────────────────────────
# Every value below is the default. Uncomment only to change one.
#
# The Minecraft plugin does NOT read these: it is configured through its own
# plugins/RobticMinecraft/api.yml, which holds the base URL and the API key.

# MINECRAFT_API_HOST=0.0.0.0                        # inside a container, loopback is unreachable
# MINECRAFT_API_PORT=3002
# MINECRAFT_API_PUBLIC_URL=http://localhost:3002    # advertised as the server URL in /api/docs


# ─── Dashboard ───────────────────────────────────────────────────────────────────────────────────
# Every value below is the default. Uncomment only to change one.
#
# DASHBOARD_URL and the browser's address must match exactly, scheme included — they are a CORS
# pair, and a mismatch reads as "saving does nothing" with no error anywhere.

# DASHBOARD_API_URL=http://localhost:3003          # OAuth redirect is built from this
# DASHBOARD_URL=http://localhost:3000              # the API's only permitted CORS origin
# DASHBOARD_PUBLIC_API_URL=http://localhost:3003   # what the web app tells the browser to call
# DASHBOARD_API_PORT=3003
# DASHBOARD_SESSION_TTL_MS=604800000               # 7 days
# DASHBOARD_SECURE_COOKIES=false                   # defaults to true only when the API URL is https


# ─── Local development only ──────────────────────────────────────────────────────────────────────
# Read by docker-compose.local.yml, never by application code.

# Set TRUE to accept Mojang's EULA and let the local Paper container start.
MINECRAFT_EULA=FALSE