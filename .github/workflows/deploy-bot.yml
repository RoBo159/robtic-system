name: Deploy bot

# Used to chain behind the platform API workflow (the bot was its client); with that service
# removed from the stack, the bot deploys straight off the push like the dashboard API does.
on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      force:
        description: Redeploy even when nothing changed
        type: boolean
        default: false

permissions:
  contents: read
  packages: write

# Shared with the platform API workflow, so a manual run of one queues behind the other instead of
# running `docker compose` on the same server at the same time.
concurrency:
  group: deploy-compose
  cancel-in-progress: false

jobs:
  changes:
    name: What changed
    runs-on: ubuntu-latest
    outputs:
      deploy: ${{ steps.decision.outputs.deploy }}
      signature: ${{ steps.decision.outputs.signature }}
    steps:
      - uses: actions/checkout@v4

      - id: decision
        uses: ./.github/actions/deploy-decision
        with:
          service: bot
          force: ${{ inputs.force || false }}

  # Everything here used to be a default and is now stated outright, because the defaults pointed at
  # two files that moved into infra/docker/:
  #
  #   - `dockerfile`/`image-name` — the shared workflow builds `Dockerfile` at the repository root.
  #   - `compose-*-command` — its default `docker compose` runs with no `-f`, which resolved the
  #     root docker-compose.yml. That file is now at infra/docker/compose/, so the path is explicit.
  #
  # Every value is what the corresponding default resolved to before the move, so this publishes the
  # same image and brings up the same services. Unlike the other three workflows, the bot's commands
  # name no service: it is last in the chain, and its `pull` + `up -d` deliberately covers the whole
  # stack, by which point every image is in GHCR.
  deploy:
    needs: changes
    if: ${{ needs.changes.outputs.deploy == 'true' }}
    uses: robticorg/robtic-actions/.github/workflows/deploy.yml@main
    with:
      deploy-type: docker
      runner-labels: '["self-hosted", "robtic-deploy"]'
      environment-name: 'Robtic Server'
      environment-url: 'https://robtic.org'
      server-label: 'core.robtic.org'
      dockerfile: 'infra/docker/dockerfiles/bot.Dockerfile'
      image-name: 'ghcr.io/robticorg/robtic-system'
      compose-project-name: 'robtic-system'
      compose-env-file: '/home/robtic/robtic-system/.env'
      compose-pull-command: 'docker compose -f infra/docker/compose/docker-compose.yml -p robtic-system --env-file /home/robtic/robtic-system/.env pull'
      compose-up-command: 'docker compose -f infra/docker/compose/docker-compose.yml -p robtic-system --env-file /home/robtic/robtic-system/.env up -d'
    secrets:
      DISCORD_WEBHOOK_DEPLOY: ${{ secrets.DISCORD_WEBHOOK_DEPLOY }}

  seal:
    name: Record what was deployed
    needs: [changes, deploy]
    if: ${{ needs.deploy.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Write marker
        run: |
          mkdir -p .deploy-signatures
          printf '%s %s %s\n' "${{ github.sha }}" \
            "$(date -u +%FT%TZ)" "${{ github.run_id }}" > .deploy-signatures/bot

      # continue-on-error: a concurrent run may have claimed the same key first. That is a
      # duplicate, not a failure — the deploy already happened either way.
      - uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: .deploy-signatures/bot
          key: deploy-v1-bot-${{ needs.changes.outputs.signature }}
